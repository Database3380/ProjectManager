extends ../layout

block content
    div.main.container
        div.row.align-items-center.justify-content-center
            div.col-6
                h2 Login
                form(method='POST', action='/auth/login')
                    div.form-group
                        label(for='email') Email

                        input#email.form-control(type='email', name='email', value='dean@email.com')
                    div.form-group
                        label(for='password') Password
                        input#password.form-control(type='password', name='password', value='secret')

                    div.form-group
                        button.btn.btn-primary(type='submit') Login
